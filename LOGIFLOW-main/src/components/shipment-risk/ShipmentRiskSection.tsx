
import { ShipmentRiskClient } from './ShipmentRiskClient';

export function ShipmentRiskSection() {
  return (
    <section aria-labelledby="shipment-risk-title">
      <ShipmentRiskClient />
    </section>
  );
}
