
# This is an example file. Rename this to .env.local and add your credentials.
# IMPORTANT: .env.local should NOT be committed to your Git repository.

# Genkit/Google AI API Key for client-side and server-side calls.
# Get this from the Google Cloud Console for your project.
# Go to "APIs & Services" > "Credentials"
GOOGLE_API_KEY="YOUR_GOOGLE_AI_API_KEY_HERE"


# Firebase Admin SDK Credentials
# These are used for server-side actions like sending password reset emails.
# You can generate a service account key JSON file in the Firebase Console:
# Project Settings > Service accounts > Generate new private key
#
# IMPORTANT: In a production environment like Firebase App Hosting or Vercel,
# you should set these as secure environment variables in the hosting platform's dashboard,
# not in a .env.local file.
FIREBASE_PROJECT_ID="YOUR_FIREBASE_PROJECT_ID"
FIREBASE_CLIENT_EMAIL="YOUR_FIREBASE_CLIENT_EMAIL"
# The private key needs to be a single line with `\n` characters for newlines.
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"
